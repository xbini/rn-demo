import { COMMON_PALETTE } from './common-variable'

const primaryColor = '#2b82ff'

export const PALETTE = {
    ...COMMON_PALETTE,
    primaryColor,
    tabIconDefault: '#ccc',
    tabIconSelected: primaryColor
}
